# TCHK 个人作品集：AI 维护与交付指南

> 本文既是项目交付文档，也是后续 AI 修改本作品集时的最高优先级维护说明。
>
> 任何 AI 在修改本仓库前，都应先完整阅读本文，再读取实际源码。本文不替代源码；若描述与源码冲突，以当前源码为准，并在修改前说明差异。

## 1. 项目目标

这是黄坤（TCHK）的个人作品集网站，用于展示：

- Java、Spring Boot、Vue、React 等全栈开发能力；
- AI / LLM 应用工程和智能体开发能力；
- 政企系统、公共租赁住房平台及银行接口对接经验；
- 产品设计、项目推进、测试、部署和完整交付能力；
- 已完成项目的真实功能、技术方案与可交互界面演示。

作品集不是普通简历列表。它需要同时做到：

1. 招聘者打开后能迅速看懂个人定位；
2. 重点项目有足够的技术深度和视觉证据；
3. 页面具有鲜明、统一且可维护的视觉语言；
4. 新增项目时不破坏现有布局、动效、弹窗和项目数据；
5. 所有项目描述必须以实际源码或用户确认的信息为依据。

## 2. 当前技术形态

本项目是无构建步骤的静态网站：

- `index.html`：页面结构、首屏、证据档案、能力区、关于区、联系区和 Dialog 容器；
- `styles.css`：早期基础样式、项目模拟界面和既有业务组件样式；
- `reference-theme.css`：当前视觉重构层，覆盖首屏、导航、项目展示、响应式与新增弹窗；
- `app.js`：项目数据、筛选搜索、项目卡片、项目详情、模拟 UI、证据档案和交互逻辑；
- `CNAME`：GitHub Pages 自定义域名配置，不得随意修改；
- `CLAUDE.md`：本维护与交付指南。

没有 React、Vue、npm 或打包流程。浏览器直接加载：

```html
<link rel="stylesheet" href="styles.css">
<link rel="stylesheet" href="reference-theme.css">
<script src="app.js"></script>
```

## 3. 当前视觉系统

### 3.1 设计方向

当前网站采用“工程控制台 × 数字档案 × AI 系统蓝图”的视觉方向：

- 深黑背景作为主画布；
- 荧光绿色作为核心操作信号；
- 青色、橙色及项目专属色作为局部强调；
- 超大标题建立强识别度；
- 半透明玻璃导航和信息面板表现系统感；
- 网格、轨道、圆环和代码面板表现工程与智能体主题；
- 项目使用大幅交替卡片，而不是普通三列卡片；
- 简历中的真实交付、银行对接、智能体工程和专业认可集中在可切换的“证据档案”控制台；
- 项目详情使用沉浸式 Dialog，不跳出当前页面。

核心设计变量位于 `reference-theme.css`：

```css
:root {
  --void: #050505;
  --white: #f7f8f2;
  --signal: #baff19;
  --cyan: #70e6e3;
  --max: 1700px;
  --ease-out: cubic-bezier(.16, 1, .3, 1);
}
```

### 3.2 关键布局

1. 固定胶囊导航；
2. 全屏首屏和超大 `TCHK` 标题；
3. 首屏底部悬浮项目轨道；
4. AI 交付与智能体实践证据区；
5. 可搜索、可筛选的交替式大幅项目列表；
6. Bento 风格能力区；
7. 分栏式个人介绍；
8. 全屏联系区；
9. 项目详情、个人速览和联系方式弹窗。

### 3.3 关键动效

- 导航进入动画；
- 首屏标题裁切展开；
- 首屏背景圆环呼吸；
- 指针驱动的代码面板轻量视差；
- 项目轨道悬停展开；
- 页面滚动后首屏项目轨道整体升起；
- 项目卡片悬停、封面透视和专属色光晕；
- 技术栈标签横向循环；
- Dialog 打开、关闭和背景模糊；
- AI/OCR 工作流步骤切换；
- AI PPT 智能体工作台模拟界面；
- 证据档案自动切换、键盘切换、指针光场和数字递增；
- 顶部导航滚动进度线；
- `prefers-reduced-motion` 降低动效支持。

动效应优先使用 `transform` 和 `opacity`，避免频繁修改宽高导致重排。

## 4. 参考网站是如何被拆解的

本节和第 5 节记录的是本次改版实际采用的方法，仅供后续维护者理解历史决策，不代表以后必须使用完全相同的工具。

参考站：

`https://20260627-3eg.pages.dev/#about`

这次没有直接复制对方源码、图片或品牌内容，而是提取其设计语言，再重新实现为开发者作品集。

### 4.1 分析过程

1. 获取公开 HTML，确认它是打包后的单页应用；
2. 从 HTML 找到生产环境 CSS、JavaScript 和图片资源路径；
3. 读取生产 CSS，提取颜色变量、版心宽度、圆角、导航、首屏和项目布局；
4. 提取 `@keyframes`，识别标题裁切、遮罩显现、导航进入和滚动显现方式；
5. 从 JavaScript 资源中提取公开图片清单，判断页面内容组织；
6. 将页面拆分为“全屏首屏、悬浮项目轨道、内容区大卡、作品画廊、联系页”；
7. 读取本地作品集的 `index.html`、`styles.css` 和 `app.js`；
8. 不删除旧样式，而是新增 `reference-theme.css` 作为视觉覆盖层；
9. 在 `app.js` 中实现项目轨道、滚动状态、指针视差和项目弹窗；
10. 使用本地服务器和无头浏览器分别验收桌面端、移动端及项目详情。

### 4.2 为什么能还原动效

网页动效通常不是“看起来很玄学”，而是几个可识别机制的组合：

- `clip-path`：标题从遮罩中展开；
- `transform`：位移、透视、旋转和缩放；
- `opacity`：淡入、层级切换；
- `backdrop-filter`：玻璃导航与弹窗背景；
- `radial-gradient`：光晕和氛围；
- CSS 变量：把鼠标位置传给样式；
- `pointermove`：驱动轻量视差；
- `scroll` 或 `IntersectionObserver`：驱动滚动状态；
- `transition` 与自定义贝塞尔曲线：控制运动质感；
- `prefers-reduced-motion`：在用户要求减少动效时停用动画。

真正重要的不是复制某个动画参数，而是识别页面的视觉层级和交互节奏，然后根据本网站内容重新组织。

## 5. 实际使用过的技能和工具

### 5.1 前端设计技能

#### `frontend-design`

用于确定鲜明的视觉方向，重点约束：

- 不做模板化、同质化的 AI 风格页面；
- 使用明确的排版、色彩和空间策略；
- 让动效服务于重要层级；
- 保证页面看起来像经过设计，而不是简单套组件。

#### `ui-ux-pro-max`

用于生成作品集设计系统建议，并执行交付检查：

- 色彩和字体搭配；
- 作品集页面结构；
- Hover、Focus 和触控反馈；
- 375px、768px、1024px、1440px 响应式；
- 对比度和键盘可访问性；
- `prefers-reduced-motion`；
- 避免 Hover 导致布局跳动。

#### `react-effects`

分析阶段曾检查 React 副作用规范，但当前作品集是原生 HTML/CSS/JavaScript，没有修改 React 代码，因此没有在作品集实现中引入 React Effect。

### 5.2 页面与源码分析

- Web 内容读取：获取公开 HTML；
- 静态资源读取：分析生产环境 CSS 和 JavaScript Bundle；
- 文件搜索：定位本地项目入口、样式和脚本；
- 源码读取：确认现有业务逻辑及项目数据；
- Explore 分析：梳理 AI PPT 项目的 Java 智能体架构；
- CodeGraph：曾尝试读取 AI PPT 项目，但该项目未建立索引，随后改用文件搜索和源码阅读。

### 5.3 视觉验收

- Python `http.server` 启动本地静态服务；
- Edge / Chrome 无头模式生成桌面与移动端截图；
- Chrome DevTools Protocol 验证项目 Dialog；
- 自动检查项目数量、弹窗标题及横向溢出；
- 浏览器内打开本地链接供人工体验。

### 5.4 没有使用的能力

- 没有复制参考站图片；
- 没有下载参考站源码仓库；
- 没有生成或冒充项目截图；
- 没有使用图片生成模型替代真实项目证据；
- 没有把参考站的个人信息、文案或品牌资产放进本网站。

## 6. 当前项目数据结构

项目定义在 `app.js` 顶部的 `projects` 数组中。每个项目使用以下字段：

```js
{
  id: "唯一且稳定的英文标识",
  title: "项目名称",
  category: "业务系统",
  year: "2026",
  color: "#70e6e3",
  textColor: "#101c27",
  summary: "一段真实、具体的项目摘要",
  stack: ["Java", "Spring Boot"],
  features: ["核心功能或工程亮点"],
  role: "本人承担的角色",
  aiMode: "AI 智能体项目",
  featured: true,
  source: ""
}
```

字段说明：

- `id`：同时用于项目卡片、详情弹窗、封面类型和模拟界面映射，不得重复；
- `category`：会自动进入筛选条件；
- `year`：可使用 `2026 · COMPLETED` 等状态表达；
- `color`：项目强调色；
- `summary`：招聘者在项目卡片和详情头部首先看到的内容；
- `stack`：必须与源码依赖一致；
- `features`：建议 4 至 8 条，优先写架构、业务难点和可验证结果；
- `role`：只写真实承担的角色；
- `aiMode`：存在时会显示 AI 项目标记；
- `featured`：设为 `true` 后会被排序到项目列表和首屏轨道前部；
- `source`：只有公开且允许展示的仓库才填写。

## 7. 新增一个项目的标准流程

### 7.1 先分析项目，禁止直接编文案

新增项目之前，AI 必须：

1. 读取项目根目录的 README、依赖文件和目录结构；
2. 找到前后端入口；
3. 找到核心业务模块；
4. 找到关键工作流、接口、数据模型和第三方集成；
5. 找到测试、错误处理、权限、安全和部署实现；
6. 区分本人实现、开源基础和无法确认的内容；
7. 不确定的结论必须询问用户，不得自行补齐。

Java 项目还应按实际依赖关系检查 `pom.xml` 或 `build.gradle`、`src/main`、`src/test`、`application*.yml`、数据库迁移、Docker 和 CI 文件。AI 项目还需确认模型供应商、Prompt、结构化输出、工具调用、RAG 或向量库、异步任务、失败恢复和密钥读取方式。

只读取与项目介绍和功能验证相关的源码。不得读取、展示或复制 `.env`、真实密钥、访问令牌、依赖目录和构建产物。

### 7.2 修改 `app.js`

先确认当前源码确实存在对应数据和映射，再根据新项目的实际展示需求局部修改。通常涉及：

1. 在 `projects` 数组增加项目对象；
2. 在 `renderMockUI()` 的 `mockType` 映射中增加项目类型；
3. 在 `views` 中增加该项目的模拟 UI；
4. 在 `projectCard()` 的 `coverType` 映射中增加封面类型；
5. 如需新封面样式，在 `styles.css` 或 `reference-theme.css` 增量增加专属类。

不要机械创建不需要的结构，也不要复制其他项目后只替换标题。模拟 UI 必须体现新项目自己的业务结构。修改数组、映射或样式时使用稳定锚点进行局部编辑，不得替换整个数组、整个函数或整个样式文件。

### 7.3 是否置顶

只有以下项目建议设置 `featured: true`：

- 最新完成的重点项目；
- 与当前求职方向高度一致；
- 有足够源码证据和技术深度；
- 用户明确要求重点展示。

多个项目同时 `featured: true` 时，保持数组中的相对顺序。

### 7.4 更新 AI 实践证据区

如果新项目代表新的能力方向，可更新 `index.html` 中 `.ai-evidence`，但要遵守：

- 不重复项目详情；
- 只呈现最高层的能力证据；
- 不要因为加入一个新项目就删除另一类重要能力；
- 最多保持两至三张证据卡，避免首屏之后信息过载。

## 8. 看到新网站后，如何安全加入新的 UI 或动效

### 8.1 可以借鉴

- 信息层级；
- 页面节奏；
- 栅格和版式关系；
- 动效机制；
- 交互反馈；
- 色彩占比；
- 卡片和导航的结构模式。

### 8.2 不可以直接复制

- 对方品牌名称；
- 人物照片；
- 项目图片；
- 文案；
- Logo；
- 私有代码；
- 无授权字体文件；
- 对方独有插画和视觉资产。

公开可访问不代表允许复用。不得绕过登录、付费墙、反爬限制或其他技术保护；不得反编译并复用 Bundle 中的实现代码。受许可证、服务条款或版权限制的字体、代码和素材必须先取得授权。这里只重新实现通用设计思想，不追求像素级复刻。

### 8.3 修改策略

1. 先拆解参考站，不立即写代码；
2. 归纳设计变量、布局和动效；
3. 对照当前作品集，明确哪些保留、哪些增量覆盖；
4. 优先修改 `reference-theme.css`；
5. 不为改两行样式重写整个 `styles.css`；
6. 新交互写入现有事件委托或独立初始化函数；
7. 确保鼠标交互在触屏设备上有可用替代；
8. 确保减少动效模式可用；
9. 完成后做桌面、移动端和弹窗截图验收。

## 9. 绝对禁止事项

任何 AI 修改本项目时都必须遵守：

1. 不得删除、覆盖或注释与当前任务无关的代码；
2. 不得为了“整理代码”重写整个文件；
3. 不得删除已有项目、联系方式、错误处理和交互；
4. 不得修改 `CNAME`，除非用户明确要求更换域名；
5. 不得把 `.vercel/` 或用户私人文本意外提交；
6. 不得伪造项目数据、用户数量、性能指标或合作单位；
7. 不得将项目描述成源码中不存在的技术架构；
8. 不得使用不存在的项目截图；
9. 不得复制参考站的图片和个人内容；
10. 不得自动清理预先存在的未跟踪文件；
11. 删除超过 3 行已有逻辑前，必须先询问用户；
12. 每一处修改都必须能直接对应当前需求。
13. 不得修改、覆盖、移动或删除任务开始前已经存在的未跟踪文件；
14. 禁止使用 `git clean`、`git reset --hard`、`git restore .`、`git checkout -- .` 或未经用户要求的 `git stash`；
15. 未经用户明确要求，不得提交、推送、部署或执行其他外部写入。

## 10. 修改前检查

执行：

```powershell
git status --short
git branch --show-current
git log --oneline -6
git diff --name-only
git ls-files --others --exclude-standard
```

当前已知未跟踪内容如下，但每次执行仍须动态确认：

- `.vercel/`
- `个人作品Tchk集.txt`

仅当用户要求提交时，逐个暂存本次实际修改的文件。例如本次只修改 `app.js` 和 `CLAUDE.md` 时：

```powershell
git add -- app.js CLAUDE.md
```

禁止直接使用：

```powershell
git add .
```

## 11. 本地启动与验收

### 11.1 启动

在仓库目录执行：

```powershell
python -m http.server 4173
```

访问：

`http://127.0.0.1:4173`

### 11.2 最低自动检查

```powershell
node --check app.js
git diff --check
```

### 11.3 必测交互

1. 顶部导航锚点；
2. “探索代表项目”；
3. “快速了解我”；
4. “快速了解我 → 联系我”二次弹窗；
5. 电话和邮箱链接；
6. 项目筛选；
7. 项目搜索；
8. 首屏项目轨道；
9. 项目详情 Dialog；
10. AI/OCR 工作流步骤；
11. AI PPT 智能体模拟工作台；
12. 弹窗背景点击关闭；
13. 移动端菜单；
14. 375px 附近是否横向溢出；
15. 浏览器控制台是否出现新错误；
16. Escape 是否能关闭弹窗，关闭后焦点是否回到合理位置；
17. 仅使用键盘能否操作受影响按钮；
18. 修改前后项目数量是否符合预期。
19. “证据档案”四个分类是否都能点击和键盘切换，内容是否与简历一致。
20. 页面是否在无法访问 Google Fonts 时仍能立即完成项目与交互初始化。
21. “代表工程”默认展示 5 个核心项目，“全部”仍能展示完整 18 个项目。
22. 有 `source` 的项目显示公开源码入口，无 `source` 的项目保持未公开状态。
23. 项目模拟界面必须保留“基于真实业务结构重建、不包含生产数据”的说明。

### 11.4 必测尺寸

- 375 × 812；
- 390 × 844；
- 768 × 1024；
- 1440 × 1000；
- 1920 × 1080。

### 11.5 视觉检查

- 导航是否遮住正文；
- 首屏项目轨道是否在第一屏底部可见；
- 超大标题是否在移动端被裁切；
- 项目长标题是否溢出；
- Hover 是否造成布局跳动；
- 弹窗内容是否能滚动；
- 文字和背景对比度是否足够；
- 减少动效模式下内容是否仍然可见。

## 12. Git 与部署交付状态

仓库：

`D:\GitHub\Portfolio`

分支：

`main`

远端：

- GitHub：`https://github.com/QuanQ1016/tchk-portfolio.git`
- Gitee：`https://gitee.com/SUISUI1016/tchk-portfolio.git`

线上域名：

`https://tchk.xyz`

以下内容是 2026-09-16 的历史交付记录，不可代替执行时的远端检查：

```text
4c60c9c 重构作品集视觉与交互体验
a3cdc0a 新增联系方式弹窗
567d5bc 完善 AI PPT 智能体项目展示
843eeb8 精确描述智能体异步任务机制
```

GitHub 推送曾因当前网络无法连接 `github.com:443` 失败。不要将“本地已提交”误报为“线上已更新”。

执行时先检查并更新远端引用：

```powershell
git status --short
git remote -v
git fetch github main
git rev-list --left-right --count github/main...HEAD
```

如果 `git fetch` 失败，只能说“远端状态无法确认”，不能根据本地缓存断言远端状态。

只有用户明确要求发布，并且确认本次提交范围后，才执行：

```powershell
git push github main
```

推送成功后重新执行：

```powershell
git fetch github main
git rev-list --left-right --count github/main...HEAD
```

理想结果是：

```text
0  0
```

`0 0` 只能证明 Git 提交同步，不能证明 GitHub Pages 已发布。还必须单独检查 Pages 部署结果并访问 `https://tchk.xyz` 验证页面，之后才能说“线上已更新”。

## 13. 交给其他 AI 的通用提示词

以下内容可直接复制给其他 AI：

```text
请维护我的个人作品集仓库：D:\GitHub\Portfolio。

第一步必须完整阅读 D:\GitHub\Portfolio\CLAUDE.md，然后执行 git status --short、git diff --name-only 和 git ls-files --others --exclude-standard，确认现有改动和未跟踪文件。不要修改、删除或自动提交 .vercel/ 和个人作品Tchk集.txt。

本次任务：
【在这里写清楚要新增或修改的内容】

目标：
1. 保留现有所有项目、业务逻辑、Dialog、筛选、搜索、移动端菜单和动效。
2. 只做与本次任务直接相关的增量修改。
3. 不要重写整个 styles.css；新视觉优先增量写入 reference-theme.css。
4. 不得使用“省略原代码”或占位符，不得伪造项目功能、数据和技术栈。
5. 删除超过 3 行现有逻辑前先询问我。
6. 修改前先读取相关源码；新增项目时还要读取该项目仓库的 README、依赖、入口、核心业务、测试和部署文件。
7. 如果我提供参考网站，只提取布局、设计语言和动效机制，不复制对方图片、文案、品牌和私有代码。
8. 完成后执行 node --check app.js、git diff --check，并本地启动页面。
9. 验证桌面端、390px 移动端、项目详情和所有受影响交互。
10. 最后列出修改文件、验证结果、未完成事项和是否已经推送上线。没有推送成功时不得说线上已更新。
11. 未经我明确要求，不要提交、推送或部署。

先分析并说明你准备改哪些位置，再开始修改。
```

## 14. 新增项目专用提示词

```text
请把我的新项目加入个人作品集。

作品集仓库：D:\GitHub\Portfolio
新项目源码路径：【填写新项目绝对路径】

请先完整阅读 D:\GitHub\Portfolio\CLAUDE.md，再按入口、依赖和调用关系读取与项目介绍相关的源码。不要只看 README，也不要根据项目名称猜测功能；不要读取或输出 .env、真实密钥、依赖目录和构建产物。

你需要完成：
1. 梳理前后端技术栈、核心业务流程、关键类/函数、第三方集成、权限安全、异步任务、测试与部署方式。
2. 区分真实实现和无法确认的内容，不得伪造量化数据。
3. 在 app.js 的 projects 数组中新增准确的项目对象。
4. 为项目增加专属 mockType、详情模拟 UI 和 coverType，不能复制其他项目后只换标题。
5. 根据项目重要程度判断是否 featured；不确定时先问我。
6. 如项目代表新的能力方向，可增量更新 index.html 的能力证据，但不得删除其他重要能力。
7. 保持 reference-theme.css、现有布局和所有交互兼容。
8. 执行脚本语法、差异、响应式、项目弹窗和移动端验收。
9. 未经用户明确要求不得提交；如用户要求提交，只提交本次实际修改文件，不得提交已有未跟踪文件。

项目完成状态：【进行中 / 已完成 / 已上线】
我的主要贡献：【填写本人实际承担的内容；如需从 Git 记录分析，请明确说明】
是否公开源码：【是 / 否】
希望重点投递方向：【Java 后端 / AI 应用 / 全栈 / 项目管理】
```

## 15. 参考网站改版专用提示词

```text
请参考一个公开网站，为我的作品集增加或调整 UI、布局和动效。

作品集仓库：D:\GitHub\Portfolio
参考网站：【填写网址】
希望借鉴的部分：【首屏 / 导航 / 项目布局 / 卡片 / 滚动动效 / 联系页】

第一步必须阅读 D:\GitHub\Portfolio\CLAUDE.md 和现有 index.html、styles.css、reference-theme.css、app.js。

执行要求：
1. 先获取公开 HTML，分析页面公开加载的 CSS/JS 资源结构、设计变量、关键布局和 @keyframes，但不得复制 Bundle 实现代码。
2. 输出“参考站设计机制 → 本作品集对应实现”的映射。
3. 只借鉴设计语言和交互机制，不复制对方图片、Logo、人物信息、文案和品牌资产。
4. 保留本作品集真实项目内容和数据结构。
5. 优先通过 reference-theme.css 增量覆盖，不重写 styles.css。
6. JavaScript 动效必须有移动端和 prefers-reduced-motion 兼容。
7. 不得为了视觉改版破坏筛选、搜索、Dialog、项目详情和联系方式。
8. 用无头浏览器检查桌面端、移动端和弹窗截图。
9. 明确说明实际使用了哪些技能、工具和分析方法。
10. 最后提供修改文件、视觉变化、交互变化、验证结果和部署状态。
```

## 16. 小范围修复专用提示词

```text
请修复 D:\GitHub\Portfolio 中的这个问题：

【写明复现步骤、当前结果和期望结果】

修改前先阅读 CLAUDE.md 和相关代码。只修复这个问题，不进行邻近重构、格式化或视觉升级。保留所有既有逻辑。

完成后：
1. 说明根因；
2. 列出精确修改文件；
3. 执行 node --check app.js 和 git diff --check；
4. 按复现步骤重新验证；
5. 不要提交 .vercel/ 和个人作品Tchk集.txt。
```

## 17. 交付报告模板

其他 AI 完成修改后，必须按以下格式交付：

```text
任务结果：
【已完成 / 部分完成 / 被阻塞】

修改文件：
- 【文件路径】：【修改内容】

保留内容：
- 【确认未被破坏的现有逻辑】

验证：
- node --check app.js：【通过 / 失败】
- git diff --check：【通过 / 失败】
- 桌面端：【通过 / 失败】
- 移动端：【通过 / 失败】
- 受影响交互：【通过 / 失败】

Git：
- 当前分支：【分支】
- 提交：【提交哈希或未提交】
- 推送：【已推送 / 未推送及原因】

线上状态：
- 【已更新并验证 / 尚未更新 / 无法确认及原因】

剩余事项：
- 【没有则写“无”】
```

## 18. 给维护者的最终判断标准

一次合格的作品集修改必须同时满足：

1. 内容真实；
2. 视觉统一；
3. 改动范围可解释；
4. 旧功能未丢失；
5. 桌面与移动端可用；
6. 交互有明确反馈；
7. 差异中没有无关格式化；
8. 未跟踪的用户文件没有被误提交；
9. 本地、Git 和线上状态表述准确；
10. 下一位维护者只看本文和源码就能继续工作。
